const paths={heart:'M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.7l-1.1-1.1a5.5 5.5 0 0 0-7.8 7.8L12 21l8.8-8.6a5.5 5.5 0 0 0 0-7.8Z',compass:'M12 22a10 10 0 1 0 0-20 10 10 0 0 0 0 20ZM16 8l-2.5 5.5L8 16l2.5-5.5Z',chat:'M21 11.5a8.4 8.4 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.4 8.4 0 0 1-3.8-.9L3 21l1.9-5.7a8.4 8.4 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.4 8.4 0 0 1 3.8-.9h.5a8.5 8.5 0 0 1 8 8v.5Z',user:'M20 21v-2a7 7 0 0 0-14 0v2M12 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z',pin:'M20 10c0 6-8 12-8 12S4 16 4 10a8 8 0 0 1 16 0ZM12 13a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z',filter:'M4 7h16M4 17h16M8 4v6M16 14v6',check:'M5 12l4 4L19 6',close:'M6 6l12 12M6 18 18 6',star:'m12 2 3.1 6.3 6.9 1-5 4.9 1.2 6.9L12 17.8l-6.2 3.3L7 14.2 2 9.3l6.9-1Z',undo:'M3 10a9 9 0 1 1 1 8M3 3v7h7',chevron:'m9 5 7 7-7 7',arrow:'M4 12h16m-6-6 6 6-6 6',sparkle:'m12 3 2.7 6.3L21 12l-6.3 2.7L12 21l-2.7-6.3L3 12l6.3-2.7ZM20 2v4M18 4h4',coffee:'M3 8h13v10a3 3 0 0 1-3 3H6a3 3 0 0 1-3-3ZM16 8h2a3 3 0 0 1 0 6h-2M6 2v3M10 2v3M14 2v3',shield:'M12 22s9-4 9-11V4l-9-3-9 3v7c0 7 9 11 9 11ZM8 11l3 3 5-5',send:'m22 2-7 20-4-9-9-4 20-7ZM22 2 11 13',briefcase:'M9 6V3h6v3M3 6h18v14H3ZM3 11c6 4 12 4 18 0M12 11v4'};
function icon(name){return `<svg viewBox="0 0 24 24" aria-hidden="true"><path d="${paths[name]||paths.heart}"/></svg>`}
function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
const people=[{id:1,name:'知夏',age:26,job:'品牌设计师',km:3,area:'徐汇区',image:'https://images.unsplash.com/photo-1668049221510-2bb098f74944?auto=format&fit=crop&w=900&q=85',headline:'把普通日子，过得有趣一点。',bio:'在城市里收集日落，也收集好喝的咖啡。偶尔跳出计划，去一个没去过的地方。',tags:['咖啡探索','胶片摄影','周末徒步','独立音乐','看展'],prompt:'理想的周末是…',answer:'睡到自然醒，找一家街角咖啡馆，和喜欢的人虚度一个下午。',match:96},{id:2,name:'安然',age:25,job:'插画师',km:5,area:'静安区',image:'https://images.unsplash.com/photo-1621390798053-3e24c14ea5ce?auto=format&fit=crop&w=900&q=85',headline:'世界很大，一起走走吧。',bio:'喜欢在散步时发现新的小店。养了一只脾气很好的猫，正在学着把日子画下来。',tags:['看展','城市漫步','猫咪','咖啡探索','旅行'],prompt:'最想和你一起做的事…',answer:'各自选一本书，在有阳光的地方交换阅读。',match:92},{id:3,name:'鹿鹿',age:28,job:'产品经理',km:8,area:'长宁区',image:'https://images.unsplash.com/photo-1565249469420-0e99a048f2c6?auto=format&fit=crop&w=900&q=85',headline:'认真生活，也保留一点冒险。',bio:'工作之外，常常在公园或山里。会做三道拿手菜，想找到愿意一起尝试第四道的人。',tags:['周末徒步','做饭','旅行','独立音乐','跑步'],prompt:'一个关于我的小秘密…',answer:'旅行前会做好攻略，但最难忘的总是计划外的相遇。',match:89}];
const myInterests=['咖啡探索','胶片摄影','看展','周末徒步'];
let page='bazi',mode='for-you',index=0;let likes=new Set(),skipped=new Set(),history=[];let filter={min:18,max:100,distance:10000};let profile={name:'小林',age:27,city:'上海',job:'产品设计师',bio:'喜欢咖啡、看展和随性出发的周末。希望遇到可以一起分享日常的人。'};let activeChat=2;let messages={2:[{me:false,text:'嗨！发现我们都喜欢看展。'},{me:false,text:'最近有看到什么喜欢的展览吗？'}],3:[{me:false,text:'周末要不要一起去公园走走？'}]};let toastTimer;const drafts={};
const app=document.getElementById('app'),dialog=document.getElementById('dialog');
function pool(){let list=people.filter(p=>adultAge(demoBirths[p.id].date)>=filter.min&&adultAge(demoBirths[p.id].date)<=filter.max&&(p.km===null||p.km<=filter.distance)&&!skipped.has(p.id)&&!likes.has(p.id));if(mode==='for-you')list.sort((a,b)=>(candidateMatch(b).score??0)-(candidateMatch(a).score??0)||b.tags.filter(t=>myInterests.includes(t)).length-a.tags.filter(t=>myInterests.includes(t)).length);if(mode==='nearby')list.sort((a,b)=>a.km-b.km);if(mode==='interests')list.sort((a,b)=>b.tags.filter(t=>myInterests.includes(t)).length-a.tags.filter(t=>myInterests.includes(t)).length);return list}
function current(){const list=pool();return list.length?list[index%list.length]:null}
function toast(text){clearTimeout(toastTimer);const el=document.getElementById('toast');el.textContent=text;el.classList.add('show');toastTimer=setTimeout(()=>el.classList.remove('show'),2600)}
function heading(title,description,action=''){return `<div class="page-heading"><div><div class="eyebrow">A LITTLE CLOSER</div><h1>${title}</h1><p>${description}</p></div>${action}</div>`}
function render(){document.querySelectorAll('[data-page]').forEach(el=>el.classList.toggle('active',el.dataset.page===page));document.getElementById('like-count').textContent=likes.size;document.querySelector('.account strong').textContent=profile.name;document.getElementById('breadcrumb').textContent={bazi:'合缘 / 生辰匹配',discover:'发现 / 合缘推荐',likes:'喜欢 / 心动收藏',messages:'聊天 / 让故事继续',profile:'我的 / 个人资料'}[page];app.innerHTML=`<div class="scroll-in">${page==='bazi'?renderBazi():page==='discover'?renderDiscover():page==='likes'?renderLikes():page==='messages'?renderMessages():renderProfile()}</div>`;document.querySelectorAll('[data-icon]').forEach(el=>el.innerHTML=icon(el.dataset.icon));if(page==='messages'){const el=document.querySelector('.chat-messages');if(el)el.scrollTop=el.scrollHeight}}
function renderDiscover(){const p=current();return heading('发现你的同频','有趣的人，正在与你的生活靠近。',`<button class="outline-btn" data-action="filter">${icon('filter')}偏好设置</button>`)+`<div class="tabs" role="tablist" aria-label="推荐方式">${[['for-you','合缘推荐'],['nearby','附近的人'],['interests','共同兴趣']].map(([id,name])=>`<button class="tab ${mode===id?'active':''}" role="tab" aria-selected="${mode===id}" data-mode="${id}">${name}</button>`).join('')}<span class="day-count">${p?'今日精选 · '+pool().length+' 位':'今日精选'}</span></div>`+(p?`<div class="discover-grid"><section aria-label="推荐人物"><div class="portrait-card" id="swipe-card"><img src="${p.image}" alt="${esc(p.name)}的资料头像" fetchpriority="high"><div class="card-top"><div class="photo-dots" aria-hidden="true"><span></span></div><span class="compatibility">${icon('sparkle')}${candidateMatch(p).score===null?'时辰待补充':candidateMatch(p).score+' 分 · 合缘'}</span></div><div class="card-person"><h2>${esc(p.name)}<span>${adultAge(demoBirths[p.id].date)}</span><i class="verified" title="资料">${icon('check')}</i></h2><p>${icon('pin')}<span>${p.km===null?'距离未填写':p.km+' km'} · ${esc(p.area)}</span><span class="person-job">${esc(p.job)}</span></p><div class="photo-tags"><span>认真认识</span><span>${esc(p.tags[0]||'等待了解')}</span><span>${esc(p.tags[1]||'慢慢认识')}</span></div></div></div><div class="actions"><div class="action-group"><button class="circle-button undo" data-action="undo" aria-label="撤回上一次选择">${icon('undo')}</button><small>撤回</small></div><div class="action-group"><button class="circle-button" data-action="skip" aria-label="暂时跳过">${icon('close')}</button><small>跳过</small></div><div class="action-group"><button class="circle-button like" data-action="like" aria-label="喜欢${esc(p.name)}">${icon('heart')}</button><small>喜欢</small></div><div class="action-group"><button class="circle-button star" data-action="super" aria-label="特别喜欢${esc(p.name)}">${icon('star')}</button><small>特别喜欢</small></div></div><p class="keyboard-hint">← 跳过 · 喜欢 → &nbsp; 或滑动卡片</p></section><aside class="profile-aside">${baziProfileStrip(p)}<p class="section-kicker">关于 ${esc(p.name)}</p><h3>${esc(p.headline)}</h3><p class="intro">${esc(p.bio)}</p><h4 class="subheading">${icon('sparkle')}我们可能聊得来</h4><div class="chips">${p.tags.map((t,i)=>`<span class="chip ${myInterests.includes(t)?'shared':''}">${esc(t)}</span>`).join('')}</div><p class="shared-note">粉色标签是你们的共同兴趣</p><div class="section-rule"></div><div class="prompt-card"><span class="quote-mark" aria-hidden="true">“</span><small>${esc(p.prompt)}</small><p>${esc(p.answer)}</p></div><button class="start-chat" data-contact="${p.id}">查看联系信息 ${icon('arrow')}</button><div class="dating-goal">${icon('heart')}期待一段自然、认真的关系</div><div class="aside-bottom">${icon('shield')}自在表达，尊重彼此<button class="view-link" data-action="detail">了解更多</button></div></aside></div>`:`<div class="empty">${icon('sparkle')}<h2>今天的相遇，先到这里</h2><p>可以回顾喜欢的人，或调整偏好，发现更多可能。</p><button class="primary-btn" data-action="reset">重新浏览推荐</button><button class="outline-btn" data-action="filter">调整偏好</button>${history.length?'<button class="start-chat" data-action="undo">撤回上一次选择</button>':''}</div>`)}
function renderLikes(){return heading('心动，值得记住','收藏每一次「想再认识一点」的感觉。')+(likes.size?`<div class="likes-grid">${people.filter(p=>likes.has(p.id)).map(p=>`<article class="mini-person"><img src="${p.image}" alt="${esc(p.name)}的资料头像"><div class="mini-person-content"><h2>${esc(p.name)}，${p.age}</h2><p>${esc(p.job)} · 距离 ${p.km===null?'距离未填写':p.km+' km'}</p><button class="primary-btn" data-contact="${p.id}">联系方式 ${icon('chat')}</button><button class="start-chat" data-unlike="${p.id}">取消喜欢</button></div></article>`).join('')}</div>`:`<div class="empty">${icon('heart')}<h2>把心动留在这里</h2><p>在发现页点击爱心，喜欢的人就会出现在这里。</p><button class="primary-btn" data-page="discover">去发现有趣的人 ${icon('arrow')}</button></div>`)}
function renderMessages(){const chats=Object.keys(messages).map(Number),p=people.find(p=>p.id===activeChat)||people[1];return heading('让故事，继续发生','从一句简单的「你好」开始。')+`<div class="chat-layout"><aside class="conversation-list" aria-label="对话列表">${chats.map(id=>{const person=people.find(p=>p.id===id);return `<button class="conversation ${activeChat===id?'selected':''}" data-conversation="${id}"><img src="${person.image}" alt=""><span><strong>${person.name}</strong><small>${esc(messages[id].at(-1)?.text||'开启你们的第一句对话')}</small></span></button>`}).join('')}</aside><section class="chat-panel"><div class="chat-header"><img src="${p.image}" alt=""><span><strong>${esc(p.name)}</strong><small>${p.tags.slice(0,2).join(' · ')}</small></span><span class="chat-demo">演示聊天</span></div><div class="chat-messages" role="log" aria-label="聊天记录"><div class="chat-date">今天 · 对话仅作交互演示</div>${(messages[p.id]||[]).map(m=>`<div class="bubble ${m.me?'me':''}">${esc(m.text)}</div>`).join('')}${!(messages[p.id]||[]).length?'<p class="muted">你们的故事，从这里开始。</p>':''}</div><form class="chat-form" id="chat-form"><input name="message" aria-label="聊天消息" maxlength="500" autocomplete="off" value="${esc(drafts[p.id]||'')}" placeholder="说点什么，让彼此更近一点…" required><button class="primary-btn" type="submit" aria-label="发送消息">${icon('send')}</button></form></section></div>`}
function renderProfile(){return heading('真实的你，自带吸引力','不必完美，只需要做自己。')+`<section class="profile-sheet"><div class="my-profile-header"><span class="self-avatar">${esc(profile.name.slice(0,1))}</span><div><h2>${esc(profile.name)}，${profile.age}</h2><span class="muted">${esc(profile.city)} · ${esc(profile.job)}</span></div></div><h4 class="subheading">关于我</h4><p class="intro">${esc(profile.bio)}</p><h4 class="subheading">我的兴趣</h4><div class="chips"><span class="chip shared">咖啡探索</span><span class="chip shared">胶片摄影</span><span class="chip">看展</span><span class="chip">周末徒步</span></div><div class="profile-detail"><span>交友期待</span><span>认真认识，自然发展</span></div><div class="profile-detail"><span>所在城市</span><span>${esc(profile.city)}</span></div><button class="bazi-inline" data-page="bazi"><span>${icon('sparkle')}我的八字与合缘推荐</span>${icon('chevron')}</button><button class="primary-btn" data-action="edit-profile">编辑个人资料</button><p class="muted">这是演示资料。当前修改会在刷新后重置。</p></section>`}
function navigate(next){page=next;render();window.scrollTo({top:0,behavior:'instant'})}
function modal(content){document.getElementById('dialog-content').innerHTML=content;dialog.showModal()}
function closeButton(){return `<button class="icon-btn" data-action="close-dialog" aria-label="关闭弹窗">${icon('close')}</button>`}
function decide(kind){const p=current();if(!p)return;history.push({id:p.id,kind});if(kind==='skip'){skipped.add(p.id);toast('已经跳过，可以点击撤回')}else{likes.add(p.id);toast(`已${kind==='super'?'特别':''}喜欢 ${esc(p.name)}`)}index=0;render();if(kind==='super')modal(`<div class="match-dialog">${icon('sparkle')}<h2>让这次心动，更特别</h2><img src="${p.image}" alt="${esc(p.name)}"><p class="muted">你特别喜欢了 ${esc(p.name)}。<br>用一句真诚的问候，开启你们的故事。</p><button class="primary-btn full" data-contact="${p.id}">联系方式 ${icon('chat')}</button><button class="outline-btn full" data-action="close-dialog">继续发现</button></div>`)}
document.addEventListener('click',e=>{const b=e.target.closest('button,a');if(!b)return;if(b.dataset.page){navigate(b.dataset.page);return}if(b.dataset.mode){mode=b.dataset.mode;index=0;render();return}if(b.dataset.chat){if(dialog.open)dialog.close();activeChat=Number(b.dataset.chat);messages[activeChat]??=[];navigate('messages');return}if(b.dataset.conversation){activeChat=Number(b.dataset.conversation);render();return}if(b.dataset.unlike){likes.delete(Number(b.dataset.unlike));history=history.filter(h=>h.id!==Number(b.dataset.unlike));render();toast('已取消喜欢');return}const a=b.dataset.action;if(['skip','like','super'].includes(a)){decide(a);return}if(a==='close-dialog')dialog.close();if(a==='undo'){const last=history.pop();if(!last){toast('还没有可以撤回的选择');return}likes.delete(last.id);skipped.delete(last.id);index=Math.max(0,pool().findIndex(p=>p.id===last.id));render();toast('已撤回上一次选择')}if(a==='reset'){skipped.clear();index=0;render();if(!pool().length)toast('推荐已全部喜欢，可在喜欢页查看或调整偏好')}if(a==='filter')openFilter();if(a==='detail'){const p=current();modal(`<div class="dialog-head"><h2>${esc(p.name)}的小档案</h2>${closeButton()}</div><p class="intro">${esc(p.bio)}</p><div class="profile-detail"><span>年龄</span><span>${p.age} 岁</span></div><div class="profile-detail"><span>职业</span><span>${esc(p.job)}</span></div><div class="profile-detail"><span>位置</span><span>${esc(p.area)} · ${p.km===null?'距离未填写':p.km+' km'}</span></div><p class="muted">资料来自你的本地候选人列表。示例照片来自 Unsplash。</p><button class="primary-btn full" data-contact="${p.id}">查看联系方式</button>`)}if(a==='edit-profile')openEdit()});
function openFilter(){modal(`<div class="dialog-head"><h2>你的交友偏好</h2>${closeButton()}</div><p class="muted">发现让你相处自在的人。</p><form id="filter-form"><div class="age-fields"><label class="field">最小年龄<input type="number" name="min" min="18" max="100" value="${filter.min}" required></label><label class="field">最大年龄<input type="number" name="max" min="18" max="100" value="${filter.max}" required></label></div><label class="field">距离范围 <output id="distance-value">${filter.distance} km</output><input type="range" name="distance" min="1" max="10000" value="${filter.distance}" oninput="document.getElementById('distance-value').textContent=this.value+' km'"></label><p class="muted">候选人由你添加；缩小范围可能没有推荐。</p><button type="submit" class="primary-btn full">保存偏好</button></form>`)}
function openEdit(){modal(`<div class="dialog-head"><h2>编辑个人资料</h2>${closeButton()}</div><form id="profile-form"><label class="field">昵称<input name="name" value="${esc(profile.name)}" maxlength="12" required></label><div class="age-fields"><label class="field">年龄<input type="number" name="age" min="18" max="100" value="${profile.age}" readonly required></label><label class="field">城市<input name="city" value="${esc(profile.city)}" maxlength="20" required></label></div><label class="field">职业<input name="job" value="${esc(profile.job)}" maxlength="25" required></label><label class="field">关于我<textarea name="bio" maxlength="200" required>${esc(profile.bio)}</textarea></label><button type="submit" class="primary-btn full">保存资料</button></form>`)}
document.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.target);if(e.target.id==='filter-form'){const min=Number(data.get('min')),max=Number(data.get('max'));if(min>max){toast('最小年龄不能大于最大年龄');return}filter={min,max,distance:Number(data.get('distance'))};index=0;dialog.close();render();toast('偏好已更新')}if(e.target.id==='profile-form'){const next=Object.fromEntries(data);if(['name','city','job','bio'].some(k=>!next[k].trim())){toast('请填写完整资料');return}profile={...next,age:Number(next.age)};dialog.close();render();toast('资料已保存，本次演示中有效')}if(e.target.id==='chat-form'){const text=data.get('message').trim();if(!text)return;messages[activeChat].push({me:true,text});drafts[activeChat]='';render();document.querySelector('#chat-form input').focus();const id=activeChat;setTimeout(()=>{messages[id].push({me:false,text:'这是一条演示回复：很开心认识你！不如先聊聊你最近喜欢的一件小事？'});if(page==='messages'&&activeChat===id){const log=document.querySelector('.chat-messages');const bubble=document.createElement('div');bubble.className='bubble';bubble.textContent=messages[id].at(-1).text;log.appendChild(bubble);log.scrollTop=log.scrollHeight;const preview=document.querySelector('[data-conversation="'+id+'"] small');if(preview)preview.textContent=bubble.textContent}},900)}});
document.addEventListener('keydown',e=>{if(page!=='discover'||dialog.open||/INPUT|TEXTAREA|SELECT/.test(e.target.tagName))return;if(e.key==='ArrowLeft'){e.preventDefault();decide('skip')}if(e.key==='ArrowRight'){e.preventDefault();decide('like')}});
document.addEventListener('input',e=>{if(e.target.matches('#chat-form input'))drafts[activeChat]=e.target.value});
let gesture=null;document.addEventListener('touchstart',e=>{gesture=e.target.closest('#swipe-card')&&e.touches.length===1?{id:e.touches[0].identifier,x:e.touches[0].clientX,y:e.touches[0].clientY}:null},{passive:true});document.addEventListener('touchcancel',()=>gesture=null,{passive:true});document.addEventListener('touchend',e=>{if(!gesture)return;const g=gesture;gesture=null;const t=Array.from(e.changedTouches).find(t=>t.identifier===g.id);if(!t||page!=='discover'||dialog.open)return;const dx=t.clientX-g.x,dy=t.clientY-g.y;if(Math.abs(dx)>80&&Math.abs(dx)>Math.abs(dy)*1.4)decide(dx>0?'like':'skip')},{passive:true});dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});
render();
if(document.modelContext?.registerTool){
  const controller=new AbortController();
  const definitions=[
    {name:'read_demo_profiles',title:'查看演示推荐',description:'Read the current fictional dating profiles and current recommendation. Does not contact real people.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:false},execute(input){if(input&&Object.keys(input).length)throw new Error('No input fields are supported');return {current:current()?.id??null,profiles:pool().map(({id,name,age,job,tags})=>({id,name,age,job,tags})),liked:Array.from(likes)}}},
    {name:'choose_current_demo_profile',title:'选择演示人物',description:'Like or skip the currently displayed fictional dating profile and advance to the next recommendation. Changes only this browser session.',inputSchema:{type:'object',properties:{profileId:{type:'integer'},choice:{type:'string',enum:['like','skip']}},required:['profileId','choice'],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:false},execute(input){if(!input||Object.keys(input).some(k=>!['profileId','choice'].includes(k))||!Number.isInteger(input.profileId)||!['like','skip'].includes(input.choice))throw new Error('Invalid profileId or choice');if(page!=='discover'||dialog.open||current()?.id!==input.profileId)throw new Error('Profile is not the visible recommendation');decide(input.choice);return {profileId:input.profileId,choice:input.choice,current:current()?.id??null}}}
  ];
  for(const tool of definitions){try{Promise.resolve(document.modelContext.registerTool(tool,{signal:controller.signal})).catch(()=>{})}catch{}}
  addEventListener('pagehide',()=>controller.abort(),{once:true});
}
